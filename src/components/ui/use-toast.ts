
// This file serves as a re-export to maintain compatibility 
// with shadcn's toast system while using sonner under the hood
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
